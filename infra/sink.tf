# The sink-agent Cloud Run service is defined in cloudrun.tf alongside the
# other three agents (scout, converter, validator) for consistency.
#
# See: infra/cloudrun.tf — google_cloud_run_v2_service "sink"
