{
    "name": "@repo/agents-scout",
    "version": "0.1.0",
    "private": true,
    "description": "Scout agent — discovers and indexes data sources",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "build": "tsc",
        "dev": "tsc --watch",
        "check-types": "tsc --noEmit",
        "lint": "eslint src --ext .ts"
    },
    "devDependencies": {
        "@repo/typescript-config": "workspace:*",
        "@types/node": "^20.17.9",
        "typescript": "^5.6.3"
    }
}