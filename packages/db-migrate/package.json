{
    "name": "@ukfreecomps/db-migrate",
    "version": "0.1.0",
    "private": true,
    "description": "Idempotent Postgres migration runner for UKFreeComps.",
    "main": "dist/migrate.js",
    "scripts": {
        "migrate": "ts-node src/migrate.ts",
        "build": "tsc",
        "start": "node dist/migrate.js"
    },
    "dependencies": {
        "dotenv": "^16.0.0",
        "pg": "^8.11.0"
    },
    "devDependencies": {
        "@types/node": "^20.0.0",
        "@types/pg": "^8.11.0",
        "ts-node": "^10.9.0",
        "typescript": "^5.0.0"
    }
}