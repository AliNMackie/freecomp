[build]
  base = "apps/web"
  command = "rm -rf .next && rm -rf node_modules/.cache && pnpm install && pnpm build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  PNPM_VERSION = "9.0.0"

[[plugins]]
  package = "@netlify/plugin-nextjs"
